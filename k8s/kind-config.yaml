kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30808
        hostPort: 30808
        protocol: TCP
      - containerPort: 32080
        hostPort: 32080
        protocol: TCP 


## kind create cluster --name airflow-airbyte --config kind-config.yaml
## if required:check (kubectl config view --minify)
    # kubectl config set-cluster kind-airflow-airbyte --server=https://127.0.0.1:6443